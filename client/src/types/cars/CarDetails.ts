export type CarDetails = {
    id: number,
    title: string,
    price: number,
    images: string[],
    carYear: number,
    fuelType: string,
    gearbox: string,
    date: string,
    city: string,
    brand: string,
    color: string 
}